# 프로젝트 작업 규칙

> 이 파일이 작업 규칙의 **단일 출처**입니다. 모든 작업에서 무조건 준수합니다.
> 규칙을 추가·변경할 때도 이 파일에 씁니다.
> (2026-08-20: 기존 `.kiro/korean-language.md`의 규칙을 이 파일로 통합. Kiro → IntelliJ 전환으로 `.kiro/` 폴더 사용 중단)

## 언어 설정
- 모든 응답은 한국어로 작성해야 합니다
- 코드 주석도 가능한 한 한국어로 작성합니다
- 기술 용어는 필요시 영어와 한국어를 병행 표기합니다 (예: "컨테이너(container)")
- 에러 메시지나 로그는 원본 언어를 유지하되, 설명은 한국어로 제공합니다

### 예외 상황
- 코드 자체는 영어로 작성 (변수명, 함수명 등)
- 공식 문서나 명령어는 원본 언어 유지
- 사용자가 명시적으로 다른 언어를 요청하는 경우에만 예외 적용

## 주석 유지 원칙
- 기존 주석은 가능한 한 유지해야 합니다.
- 해당 주석에 대응되는 코드가 삭제되어 의미가 사라진 경우에만 예외적으로 제거할 수 있습니다.
- 주석이 여전히 문맥상 의미를 가지면 코드가 변경되더라도 보존합니다.

## 코드 작성 원칙
- 코드는 최대한 직관적으로 작성해야 합니다.

## 커밋 원칙
- **커밋은 사용자가 직접 합니다** (2026-08-20 합의 — 무조건 준수).
  - **AI는 `git commit`을 실행하지 않습니다.** `git add`·`git commit`·`git push` 모두 사용자 몫입니다.
  - AI가 할 일은 두 가지: ① **커밋 메시지를 완성된 형태로 제시**(제목 + 본문, 복사해서 바로 쓸 수 있게) ② 변경 내용 보고.
  - 사용자가 "네가 커밋해"라고 명시한 경우만 예외.
  - 커밋 메시지를 제시하기 전에 `git status`를 확인해 **의도하지 않은 스테이징 변경이 섞여 있는지 먼저 알립니다.**
- 기능이 완성될 때마다 커밋합니다.
- 테스트가 통과한 상태에서만 커밋합니다 (테스트가 아직 없는 영역은 빌드/기동 확인으로 대체).

## 테스트 원칙
- 계획(PLAN.md)의 한 단계가 끝날 때마다 **전체 테스트를 실행**해 통과를 확인한 뒤 다음 단계로 진행합니다.
  - 백엔드: `gradlew build` (컴파일 + 테스트 포함)
  - 프론트엔드: ESLint + `tsc -b` + `vite build`
- 테스트가 실패하면 다음 단계로 넘어가지 않고 원인을 먼저 해결합니다.
- 테스트로 확인할 수 없는 항목(지도 렌더링 등)은 실행 화면 확인으로 대체하고, 확인한 방법을 보고합니다.

## 진행 방식 (코드 제시 → 사용자 입력 → 검사 → 진행)
> 사용자가 직접 손으로 입력하며 코드를 익히기 위한 진행 규칙. (2026-06-10 합의)

- AI는 파일을 **직접 수정하지 않는다.** 대신 **무엇을 어디에 넣을지 코드를 제시**한다.
  - 제시 단위: 한 번에 **작은 덩어리 하나**(파일 하나 또는 한 메서드/블록 단위). 한꺼번에 여러 파일을 쏟아내지 않는다.
  - 코드와 함께 **대상 파일 경로 + 위치(어느 부분에 추가/교체)**를 명확히 알려준다.
- 사용자가 **직접 입력**한 뒤 "됐다/입력했다"고 알리면, AI는 **사용자가 입력한 실제 파일 내용을 읽어 검사**한다.
  - 검사 항목: 의도대로 들어갔는지, 오타·누락·들여쓰기, 주변 코드와의 정합성.
  - 문제가 있으면 **어디가 왜 잘못됐는지 + 수정 방향**을 알려주고, 사용자가 다시 고치게 한다(정답 코드는 필요 시 제시).
- 검사를 통과하면 **다음 덩어리로 진행**한다.
- 빌드/테스트 실행은 사용자가 직접 하거나 AI가 대신 돌려 결과를 함께 확인한다(상황에 따라 합의).
- 예외: 사용자가 "이건 네가 직접 해줘"라고 명시하면 그 항목만 AI가 직접 수정한다.

### AI가 직접 처리해도 되는 것 (Phase 0 이후 운영 세칙 — 2026-06-15 합의)
- 사소·기계적 정리: 파일명 교정, 파일 이동/삭제, 깨진 설정 파일 수선, 문서(PLAN/README/plans 등) 갱신.
- 반대로 **학습 대상 코드**는 반드시 "코드 제시 → 사용자 입력 → AI 검사"로 진행: `build.gradle` 이하 빌드 설정, 엔티티/리포지토리/서비스/컨트롤러/DTO, 프론트 컴포넌트 등 로직 코드.

## 문서 체계
- **README.md** — 프로젝트 최상위 기준
- **PLAN.md** — 전체 계획 + 진행 상태. 단계 완료 시 체크박스 갱신 필수
- **plans/** — 작업 단위별 세부 계획 (설계 결정은 plans/phase0-redesign.md 기준)
- **plans/java-syntax.md**, **plans/postgis-syntax.md** — 새로 등장한 문법·패턴 정리. 학습 대상 문법이 나오면 여기에 섹션 추가
- 참고: `PLAN.md`와 `plans/`는 gitignore 대상(로컬 전용)

## AI 협업 원칙 (사용자가 주도권을 갖기 위한 규칙)

### 결정은 사용자가
- 아키텍처·라이브러리·API 설계 같은 **되돌리기 어려운 결정은 바로 실행하지 말 것.**
- 선택지 2~3개를 **트레이드오프(장단점)와 함께 제시**하고, 추천안을 명시한 뒤 사용자의 선택을 기다립니다.
- 사소한 결정(변수명, 파일 위치 등)은 알아서 진행하되 결과 보고에 근거를 한 줄 남깁니다.

### 변경 설명 의무
- 커밋 메시지를 제시할 때 반드시 함께 보고: **무엇을 바꿨는지 + 왜 그렇게 했는지 + 어떻게 검증했는지.**
- 사용자가 변경 내용을 이해하지 못한 채 커밋되는 일이 없게 합니다. 질문이 들어오면 커밋 메시지 제시보다 설명이 우선입니다.

### 작업 단위는 작게
- 큰 작업(PLAN.md 한 섹션 이상)은 시작 전에 **계획을 먼저 제시**하고 승인 후 실행합니다.
- 한 번에 하나의 단계만 진행하고, 단계 사이에 검증 게이트(전체 테스트)를 통과시킵니다.

### 검증은 사실대로
- "됐습니다"라고만 하지 말고 **실행한 검증 명령과 실제 결과**를 보여줍니다.
- 테스트 실패·미검증 항목은 숨기지 않고 명시합니다.

### 학습 모드
- 사용자가 "직접 짜볼게" 또는 "리뷰해줘"라고 하면 코드를 대신 작성하지 말고 **리뷰어 역할**로 전환합니다: 문제점 지적 + 왜 문제인지 설명 + 힌트 우선, 정답 코드는 요청 시에만.
